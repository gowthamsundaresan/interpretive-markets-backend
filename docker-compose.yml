services:
  postgres:
    image: postgres:16-alpine
    container_name: interpretive-postgres
    environment:
      POSTGRES_USER: interpretive
      POSTGRES_PASSWORD: interpretive
      POSTGRES_DB: interpretive
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
